/**
 * Next.js configuration for the usmleAI frontend.
 * Currently minimal — additional settings (rewrites, redirects, etc.)
 * will be added as the app grows.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;
