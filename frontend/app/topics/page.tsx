import TopicPicker from "@/components/topics/TopicPicker";

/**
 * Topics page route (/topics).
 * Renders the TopicPicker component which displays all study topics
 * grouped by discipline category for the user to select from.
 */
export default function TopicsPage() {
  return <TopicPicker />;
}
