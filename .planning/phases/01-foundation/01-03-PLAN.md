---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/app/seed/__init__.py
  - backend/app/seed/data/topics.json
  - backend/app/seed/data/questions_step1.json
  - backend/app/seed/data/questions_step2ck.json
  - backend/app/seed/data/questions_step3.json
  - backend/app/seed/loader.py
  - backend/app/routers/__init__.py
  - backend/app/routers/topics.py
  - backend/app/routers/questions.py
  - backend/app/routers/claude.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/knowledge.py
  - backend/app/schemas/claude.py
autonomous: true
requirements:
  - QGEN-05
  - QGEN-06
must_haves:
  truths:
    - "Knowledge bank contains seeded topics and questions for all three USMLE Steps"
    - "API returns topics and questions filtered by USMLE step"
    - "Claude API responds to a test prompt confirming integration is live"
    - "Seed data covers all 16 disciplines across basic science, clinical science, and behavioral/social"
  artifacts:
    - path: "backend/app/seed/data/topics.json"
      provides: "Topic and subtopic seed data for all 16 disciplines"
      contains: "Anatomy"
    - path: "backend/app/seed/data/questions_step1.json"
      provides: "Curated Step 1 questions"
      contains: "step1"
    - path: "backend/app/routers/topics.py"
      provides: "GET /api/topics endpoint"
      contains: "router"
    - path: "backend/app/routers/questions.py"
      provides: "GET /api/questions endpoint"
      contains: "router"
    - path: "backend/app/routers/claude.py"
      provides: "POST /api/claude/test endpoint"
      contains: "anthropic"
    - path: "backend/app/seed/loader.py"
      provides: "Database seeding script"
      contains: "def seed_database"
  key_links:
    - from: "backend/app/routers/topics.py"
      to: "backend/app/models/knowledge.py"
      via: "SQLAlchemy query"
      pattern: "Topic"
    - from: "backend/app/routers/claude.py"
      to: "backend/app/config.py"
      via: "ANTHROPIC_API_KEY"
      pattern: "settings.ANTHROPIC_API_KEY"
    - from: "backend/app/seed/loader.py"
      to: "backend/app/seed/data/"
      via: "JSON file loading"
      pattern: "json.load"
    - from: "backend/app/main.py"
      to: "backend/app/routers/"
      via: "router includes"
      pattern: "include_router"
---

<objective>
Seed the knowledge bank with curated USMLE content across all 16 disciplines and three Steps, create API endpoints to retrieve topics and questions, and integrate the Claude API for AI-powered question generation.

Purpose: Fulfill both Phase 1 requirements — QGEN-05 (all USMLE Steps covered) and QGEN-06 (hybrid knowledge bank with curated content + Claude AI integration) — making the backend fully functional.
Output: Seeded database, working REST endpoints for topics/questions, and a verified Claude API connection.
</objective>

<execution_context>
@/home/jiangpang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiangpang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed data JSON files covering all 16 USMLE disciplines and three Steps</name>
  <files>
    backend/app/seed/__init__.py,
    backend/app/seed/data/topics.json,
    backend/app/seed/data/questions_step1.json,
    backend/app/seed/data/questions_step2ck.json,
    backend/app/seed/data/questions_step3.json,
    backend/app/seed/loader.py
  </files>
  <read_first>
    backend/app/models/knowledge.py,
    backend/app/database.py
  </read_first>
  <action>
    Create seed data and loading mechanism:

    1. **backend/app/seed/__init__.py**: empty file

    2. **backend/app/seed/data/topics.json** — All 16 disciplines with subtopics:
       ```json
       [
         {
           "name": "Anatomy",
           "discipline": "basic_science",
           "description": "Gross anatomy, embryology, histology, and neuroanatomy",
           "subtopics": ["Gross Anatomy", "Embryology", "Histology", "Neuroanatomy"]
         },
         {
           "name": "Physiology",
           "discipline": "basic_science",
           "description": "Organ system physiology and cellular mechanisms",
           "subtopics": ["Cardiovascular Physiology", "Renal Physiology", "Respiratory Physiology", "Neurophysiology", "Endocrine Physiology", "GI Physiology"]
         },
         {
           "name": "Biochemistry",
           "discipline": "basic_science",
           "description": "Molecular biology, metabolism, and genetics",
           "subtopics": ["Metabolism", "Molecular Biology", "Genetics", "Nutrition"]
         },
         {
           "name": "Pathology",
           "discipline": "basic_science",
           "description": "General and systemic pathology, laboratory medicine",
           "subtopics": ["General Pathology", "Systemic Pathology", "Hematopathology", "Clinical Pathology"]
         },
         {
           "name": "Pharmacology",
           "discipline": "basic_science",
           "description": "Drug mechanisms, pharmacokinetics, and therapeutics",
           "subtopics": ["Autonomic Pharmacology", "Cardiovascular Drugs", "Antimicrobials", "CNS Pharmacology", "Endocrine Pharmacology"]
         },
         {
           "name": "Microbiology",
           "discipline": "basic_science",
           "description": "Bacteriology, virology, mycology, parasitology, and immunology",
           "subtopics": ["Bacteriology", "Virology", "Mycology", "Parasitology", "Immunology"]
         },
         {
           "name": "Internal Medicine",
           "discipline": "clinical_science",
           "description": "Diagnosis and management of adult diseases",
           "subtopics": ["Cardiology", "Pulmonology", "Gastroenterology", "Nephrology", "Hematology/Oncology", "Endocrinology", "Rheumatology", "Infectious Disease"]
         },
         {
           "name": "Surgery",
           "discipline": "clinical_science",
           "description": "Surgical principles, trauma, and operative management",
           "subtopics": ["General Surgery", "Trauma", "Surgical Emergencies", "Perioperative Care"]
         },
         {
           "name": "Pediatrics",
           "discipline": "clinical_science",
           "description": "Growth, development, and pediatric disease management",
           "subtopics": ["Neonatology", "Growth and Development", "Pediatric Infectious Disease", "Pediatric Cardiology"]
         },
         {
           "name": "Obstetrics and Gynecology",
           "discipline": "clinical_science",
           "description": "Pregnancy, labor, reproductive health, and gynecologic disorders",
           "subtopics": ["Obstetrics", "Gynecology", "Reproductive Endocrinology"]
         },
         {
           "name": "Psychiatry",
           "discipline": "clinical_science",
           "description": "Mental health disorders, psychopharmacology, and behavioral science",
           "subtopics": ["Mood Disorders", "Anxiety Disorders", "Psychotic Disorders", "Substance Use", "Psychopharmacology"]
         },
         {
           "name": "Emergency Medicine",
           "discipline": "clinical_science",
           "description": "Acute care, resuscitation, and emergency procedures",
           "subtopics": ["Trauma Management", "Cardiac Emergencies", "Toxicology", "Environmental Emergencies"]
         },
         {
           "name": "Biostatistics",
           "discipline": "behavioral_social",
           "description": "Study design, statistical analysis, and evidence-based medicine",
           "subtopics": ["Study Design", "Statistical Tests", "Evidence-Based Medicine", "Epidemiological Measures"]
         },
         {
           "name": "Epidemiology",
           "discipline": "behavioral_social",
           "description": "Disease distribution, outbreak investigation, and population health",
           "subtopics": ["Disease Surveillance", "Outbreak Investigation", "Screening", "Prevention"]
         },
         {
           "name": "Ethics",
           "discipline": "behavioral_social",
           "description": "Medical ethics principles, informed consent, and end-of-life issues",
           "subtopics": ["Autonomy", "Beneficence", "Informed Consent", "End-of-Life Care", "Confidentiality"]
         },
         {
           "name": "Patient Safety",
           "discipline": "behavioral_social",
           "description": "Quality improvement, error prevention, and healthcare systems",
           "subtopics": ["Quality Improvement", "Medical Errors", "Healthcare Systems", "Communication"]
         }
       ]
       ```

    3. **backend/app/seed/data/questions_step1.json** — At least 3 curated Step 1 questions:
       Create 3 single-best-answer vignette questions covering different basic science disciplines (e.g., Pathology, Pharmacology, Physiology). Each question object:
       ```json
       {
         "topic": "Pathology",
         "subtopic": "General Pathology",
         "stem": "A 45-year-old man presents with...[clinical vignette]...Which of the following is the most likely diagnosis?",
         "question_type": "single_best_answer",
         "usmle_step": "step1",
         "difficulty": "medium",
         "source": "curated",
         "explanation": "The correct answer is B because...[detailed explanation]",
         "learning_objectives": ["Identify key features of granulomatous inflammation", "Distinguish between caseating and non-caseating granulomas"],
         "options": [
           {"label": "A", "text": "[distractor]", "is_correct": false, "explanation": "Incorrect because..."},
           {"label": "B", "text": "[correct answer]", "is_correct": true, "explanation": "Correct because..."},
           {"label": "C", "text": "[distractor]", "is_correct": false, "explanation": "Incorrect because..."},
           {"label": "D", "text": "[distractor]", "is_correct": false, "explanation": "Incorrect because..."},
           {"label": "E", "text": "[distractor]", "is_correct": false, "explanation": "Incorrect because..."}
         ]
       }
       ```
       Write medically accurate, USMLE-style vignettes. Include proper clinical presentations, relevant lab values, and teaching-quality explanations.

    4. **backend/app/seed/data/questions_step2ck.json** — At least 3 curated Step 2 CK questions covering clinical disciplines (e.g., Internal Medicine, Pediatrics, OB/GYN). Same format as Step 1.

    5. **backend/app/seed/data/questions_step3.json** — At least 2 curated Step 3 questions covering practice-of-medicine topics (e.g., Biostatistics, Ethics). Same format.

    6. **backend/app/seed/loader.py** — Seeding script:
       ```python
       import json
       import os
       from pathlib import Path
       from sqlalchemy.orm import Session
       from app.database import SessionLocal, engine, Base
       from app.models.knowledge import Topic, Subtopic, Question, AnswerOption

       # Directory containing seed data JSON files
       SEED_DIR = Path(__file__).parent / "data"

       def seed_topics(db: Session) -> dict:
           """Load topics and subtopics from topics.json. Returns mapping of topic name -> Topic object."""
           # ...load topics.json, create Topic and Subtopic records, return name->obj mapping

       def seed_questions(db: Session, topic_map: dict, filename: str) -> int:
           """Load questions from a JSON file and insert into database. Returns count of questions loaded."""
           # ...load question JSON, match topic/subtopic by name, create Question + AnswerOption records

       def seed_database():
           """Main seeding function — creates tables and loads all seed data."""
           Base.metadata.create_all(bind=engine)
           db = SessionLocal()
           try:
               # Check if already seeded (topics exist)
               if db.query(Topic).count() > 0:
                   print("Database already seeded, skipping.")
                   return
               topic_map = seed_topics(db)
               count = 0
               for filename in ["questions_step1.json", "questions_step2ck.json", "questions_step3.json"]:
                   count += seed_questions(db, topic_map, filename)
               db.commit()
               print(f"Seeded {len(topic_map)} topics and {count} questions.")
           except Exception as e:
               db.rollback()
               raise e
           finally:
               db.close()

       if __name__ == "__main__":
           seed_database()
       ```

    Add comments explaining every function and data structure.
  </action>
  <verify>
    <automated>cd /home/jiangpang/usmleAI && docker compose up -d db && sleep 2 && cd backend && python -m app.seed.loader && python -c "
from app.database import SessionLocal
from app.models.knowledge import Topic, Question
db = SessionLocal()
topics = db.query(Topic).count()
questions = db.query(Question).count()
steps = set(q.usmle_step for q in db.query(Question).all())
print(f'Topics: {topics}, Questions: {questions}, Steps: {steps}')
assert topics >= 16, f'Expected >= 16 topics, got {topics}'
assert questions >= 8, f'Expected >= 8 questions, got {questions}'
assert steps == {'step1', 'step2ck', 'step3'}, f'Expected all 3 steps, got {steps}'
print('Seed verification PASSED')
db.close()
"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/seed/data/topics.json contains all 16 disciplines: "Anatomy", "Physiology", "Biochemistry", "Pathology", "Pharmacology", "Microbiology", "Internal Medicine", "Surgery", "Pediatrics", "Obstetrics and Gynecology", "Psychiatry", "Emergency Medicine", "Biostatistics", "Epidemiology", "Ethics", "Patient Safety"
    - backend/app/seed/data/topics.json contains "basic_science", "clinical_science", "behavioral_social" discipline values
    - backend/app/seed/data/questions_step1.json contains "step1" and at least 3 question objects
    - backend/app/seed/data/questions_step2ck.json contains "step2ck" and at least 3 question objects
    - backend/app/seed/data/questions_step3.json contains "step3" and at least 2 question objects
    - backend/app/seed/loader.py contains "def seed_database" and "def seed_topics" and "def seed_questions"
    - Running `python -m app.seed.loader` produces "Seeded" output with topic and question counts
  </acceptance_criteria>
  <done>Database seeded with 16 topics across all 3 discipline categories and at least 8 curated questions spanning Steps 1, 2 CK, and 3</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic schemas and REST API endpoints for topics, questions, and Claude test</name>
  <files>
    backend/app/schemas/__init__.py,
    backend/app/schemas/knowledge.py,
    backend/app/schemas/claude.py,
    backend/app/routers/__init__.py,
    backend/app/routers/topics.py,
    backend/app/routers/questions.py,
    backend/app/routers/claude.py,
    backend/app/main.py
  </files>
  <read_first>
    backend/app/models/knowledge.py,
    backend/app/database.py,
    backend/app/config.py,
    backend/app/main.py
  </read_first>
  <action>
    Create Pydantic response schemas, API routers, and Claude integration:

    1. **backend/app/schemas/__init__.py**: empty file

    2. **backend/app/schemas/knowledge.py** — Pydantic response models:
       ```python
       from pydantic import BaseModel
       from uuid import UUID
       from datetime import datetime
       from typing import Optional

       class SubtopicResponse(BaseModel):
           id: UUID
           name: str
           description: Optional[str]
           model_config = {"from_attributes": True}

       class TopicResponse(BaseModel):
           id: UUID
           name: str
           discipline: str
           description: Optional[str]
           subtopics: list[SubtopicResponse]
           model_config = {"from_attributes": True}

       class TopicListResponse(BaseModel):
           id: UUID
           name: str
           discipline: str
           description: Optional[str]
           model_config = {"from_attributes": True}

       class AnswerOptionResponse(BaseModel):
           id: UUID
           label: str
           text: str
           is_correct: bool
           explanation: Optional[str]
           model_config = {"from_attributes": True}

       class QuestionResponse(BaseModel):
           id: UUID
           stem: str
           question_type: str
           usmle_step: str
           difficulty: str
           source: str
           explanation: Optional[str]
           learning_objectives: Optional[str]
           topic_id: UUID
           subtopic_id: Optional[UUID]
           answer_options: list[AnswerOptionResponse]
           model_config = {"from_attributes": True}
       ```

    3. **backend/app/schemas/claude.py** — Claude API schemas:
       ```python
       from pydantic import BaseModel

       class ClaudeTestRequest(BaseModel):
           prompt: str = "Explain the pathophysiology of heart failure in one paragraph."

       class ClaudeTestResponse(BaseModel):
           response: str
           model: str
           status: str
       ```

    4. **backend/app/routers/__init__.py**: empty file

    5. **backend/app/routers/topics.py**:
       - APIRouter with prefix="/api/topics", tags=["topics"]
       - GET "/" — List all topics. Optional query param `step` (str) to filter by USMLE step (if provided, only return topics that have at least one question with that step). Optional `discipline` (str) to filter by discipline. Returns list[TopicListResponse].
       - GET "/{topic_id}" — Get single topic with its subtopics. Returns TopicResponse. 404 if not found.

    6. **backend/app/routers/questions.py**:
       - APIRouter with prefix="/api/questions", tags=["questions"]
       - GET "/" — List questions. Optional query params: `topic_id` (UUID), `usmle_step` (str), `difficulty` (str), `question_type` (str), `limit` (int, default 10, max 50). Returns list[QuestionResponse].
       - GET "/{question_id}" — Get single question with answer options. Returns QuestionResponse. 404 if not found.

    7. **backend/app/routers/claude.py**:
       - APIRouter with prefix="/api/claude", tags=["claude"]
       - POST "/test" — Test Claude API connection. Accepts ClaudeTestRequest body. Creates Anthropic client using settings.ANTHROPIC_API_KEY. Calls claude-sonnet-4-20250514 model with the prompt (max_tokens=500). Returns ClaudeTestResponse with the response text, model name, and status "success". If API key missing or call fails, return appropriate error (400 for missing key, 502 for API failure).
       - Implementation:
         ```python
         from anthropic import Anthropic
         from app.config import settings

         @router.post("/test", response_model=ClaudeTestResponse)
         async def test_claude(request: ClaudeTestRequest):
             if not settings.ANTHROPIC_API_KEY:
                 raise HTTPException(status_code=400, detail="ANTHROPIC_API_KEY not configured")
             try:
                 client = Anthropic(api_key=settings.ANTHROPIC_API_KEY)
                 message = client.messages.create(
                     model="claude-sonnet-4-20250514",
                     max_tokens=500,
                     messages=[{"role": "user", "content": request.prompt}]
                 )
                 return ClaudeTestResponse(
                     response=message.content[0].text,
                     model=message.model,
                     status="success"
                 )
             except Exception as e:
                 raise HTTPException(status_code=502, detail=f"Claude API error: {str(e)}")
         ```

    8. **Update backend/app/main.py** — Register all routers:
       - Add imports: `from app.routers import topics, questions, claude`
       - Add: `app.include_router(topics.router)`
       - Add: `app.include_router(questions.router)`
       - Add: `app.include_router(claude.router)`
       - Keep the existing startup event and health check

    Add comments explaining every endpoint, query parameter, and significant code point.
  </action>
  <verify>
    <automated>cd /home/jiangpang/usmleAI && docker compose up -d db && sleep 2 && cd backend && python -m app.seed.loader 2>/dev/null; uvicorn app.main:app --host 0.0.0.0 --port 8000 &
sleep 3
curl -s http://localhost:8000/ | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='healthy'; print('Health OK')"
curl -s http://localhost:8000/api/topics/ | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d)>=16; print(f'Topics: {len(d)}')"
curl -s "http://localhost:8000/api/questions/?usmle_step=step1" | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d)>=1; print(f'Step1 questions: {len(d)}')"
curl -s "http://localhost:8000/api/questions/?usmle_step=step2ck" | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d)>=1; print(f'Step2CK questions: {len(d)}')"
curl -s "http://localhost:8000/api/questions/?usmle_step=step3" | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d)>=1; print(f'Step3 questions: {len(d)}')"
kill %1 2>/dev/null
echo "All endpoint checks PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/knowledge.py contains "class TopicResponse" and "class QuestionResponse" and "class AnswerOptionResponse"
    - backend/app/schemas/claude.py contains "class ClaudeTestRequest" and "class ClaudeTestResponse"
    - backend/app/routers/topics.py contains "APIRouter" and 'prefix="/api/topics"' and "def " (at least 2 endpoint functions)
    - backend/app/routers/questions.py contains "APIRouter" and 'prefix="/api/questions"' and "usmle_step"
    - backend/app/routers/claude.py contains "Anthropic" and "ANTHROPIC_API_KEY" and "claude-sonnet-4-20250514"
    - backend/app/main.py contains "include_router" for topics, questions, and claude routers
    - GET /api/topics/ returns 16+ topics
    - GET /api/questions/?usmle_step=step1 returns at least 1 question
    - GET /api/questions/?usmle_step=step2ck returns at least 1 question
    - GET /api/questions/?usmle_step=step3 returns at least 1 question
  </acceptance_criteria>
  <done>All API endpoints functional — topics and questions retrievable by USMLE step, Claude test endpoint configured. Routers registered in main app.</done>
</task>

</tasks>

<verification>
- Database contains 16 topics across 3 discipline categories
- GET /api/topics/ returns all topics
- GET /api/questions/?usmle_step=step1 returns Step 1 questions
- GET /api/questions/?usmle_step=step2ck returns Step 2 CK questions
- GET /api/questions/?usmle_step=step3 returns Step 3 questions
- POST /api/claude/test returns 400 if API key not set, or success if configured
- All question objects include answer options with explanations
</verification>

<success_criteria>
- Knowledge bank seeded with 16 disciplines and 8+ curated questions across all 3 USMLE Steps
- REST endpoints return topics filtered by discipline and questions filtered by USMLE step
- Claude API integration endpoint exists and validates API key presence
- Seed data includes medically accurate vignettes with teaching-quality explanations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
