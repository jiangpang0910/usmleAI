---
phase: quick
plan: 260320-l75
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user.py
  - backend/app/routers/auth.py
  - backend/app/auth.py
  - backend/app/config.py
  - backend/app/main.py
  - backend/requirements.txt
  - backend/.env
  - frontend/lib/types.ts
  - frontend/lib/api.ts
  - frontend/lib/auth.ts
  - frontend/components/dashboard/Dashboard.tsx
autonomous: true
requirements: []
user_setup:
  - service: google-oauth
    why: "Google OAuth login for user authentication"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> Create OAuth 2.0 Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client ID secret"
      - name: JWT_SECRET
        source: "Generate with: python -c \"import secrets; print(secrets.token_hex(32))\""
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID (Web application type)"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
      - task: "Add http://localhost:8000/api/auth/google/callback to Authorized redirect URIs"
        location: "Google Cloud Console -> OAuth 2.0 Client ID settings"
      - task: "Add http://localhost:3000 to Authorized JavaScript origins"
        location: "Google Cloud Console -> OAuth 2.0 Client ID settings"
must_haves:
  truths:
    - "User can click 'Sign in with Google' on the dashboard header"
    - "Google OAuth redirects back and creates a User row in PostgreSQL"
    - "JWT token is stored in browser and sent with subsequent API requests"
    - "Dashboard header shows user email/name when logged in"
    - "User can sign out and return to anonymous state"
  artifacts:
    - path: "backend/app/models/user.py"
      provides: "User SQLAlchemy model with google_id, email, name, picture_url"
    - path: "backend/app/routers/auth.py"
      provides: "OAuth login/callback/me/logout endpoints"
      exports: ["router"]
    - path: "backend/app/auth.py"
      provides: "JWT creation, verification, and optional auth dependency"
      exports: ["create_access_token", "get_current_user_optional", "get_current_user"]
    - path: "frontend/lib/auth.ts"
      provides: "Auth context with useAuth hook for login state"
  key_links:
    - from: "frontend/components/dashboard/Dashboard.tsx"
      to: "/api/auth/google/login"
      via: "Sign in button href redirect"
      pattern: "api/auth/google/login"
    - from: "backend/app/routers/auth.py"
      to: "backend/app/models/user.py"
      via: "SQLAlchemy query to find_or_create user after OAuth callback"
      pattern: "db\\.query\\(User\\)"
    - from: "frontend/lib/api.ts"
      to: "localStorage token"
      via: "Authorization header on every apiFetch call"
      pattern: "Bearer.*token"
---

<objective>
Add Google OAuth login to usmleAI with a Users table in PostgreSQL, OAuth flow via FastAPI + authlib, JWT session tokens, a "Sign in with Google" button on the dashboard header, and optional auth middleware on API routes.

Purpose: Enable user identity so study progress can be tied to individual accounts (prerequisite for multi-user features).
Output: Working Google OAuth login/logout flow with User model and JWT auth infrastructure.
</objective>

<execution_context>
@/home/jiangpang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiangpang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/app/config.py
@backend/app/main.py
@backend/app/database.py
@backend/app/models/knowledge.py
@backend/app/models/session.py
@frontend/components/dashboard/Dashboard.tsx
@frontend/lib/api.ts
@frontend/lib/types.ts

<interfaces>
<!-- Existing patterns the executor must follow -->

From backend/app/database.py:
```python
from app.database import Base, engine, get_db, SessionLocal
# Models inherit from Base
# Routes use Depends(get_db) for sessions
```

From backend/app/config.py:
```python
from app.config import settings
# Add new fields to Settings class with env var names
# .env file is at backend/.env
```

From backend/app/main.py:
```python
# Models imported at top for Base.metadata.create_all
# Routers registered with app.include_router(router)
# CORS already configured for localhost:3000 with credentials=True
```

From backend/app/models/knowledge.py (model pattern):
```python
# UUID primary keys: Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
# DateTime with timezone: Column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc))
# Every model has created_at and updated_at columns
# Comments on every column explaining purpose
```

From frontend/lib/api.ts:
```typescript
export async function apiFetch<T>(endpoint: string, options?: RequestInit): Promise<T>
// Uses API_BASE_URL + endpoint
// Sets Content-Type: application/json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — User model, OAuth endpoints, JWT auth middleware</name>
  <files>
    backend/app/models/user.py,
    backend/app/auth.py,
    backend/app/routers/auth.py,
    backend/app/config.py,
    backend/app/main.py,
    backend/requirements.txt,
    backend/.env
  </files>
  <action>
    1. Add authlib and python-jose[cryptography] to backend/requirements.txt, then pip install them.

    2. Add three new settings to backend/app/config.py Settings class:
       - GOOGLE_CLIENT_ID: str = "" (Google OAuth client ID)
       - GOOGLE_CLIENT_SECRET: str = "" (Google OAuth client secret)
       - JWT_SECRET: str = "dev-secret-change-me" (JWT signing key, default for dev)
       - JWT_ALGORITHM: str = "HS256" (JWT algorithm, not from env, just a class constant)
       - JWT_EXPIRY_HOURS: int = 24 (token lifetime)

    3. Create backend/app/models/user.py following the exact pattern from knowledge.py:
       - class User(Base), tablename "users"
       - id: UUID primary key (uuid.uuid4 default)
       - google_id: String(100), unique, nullable=False (Google's sub claim)
       - email: String(255), unique, nullable=False
       - name: String(255), nullable=True (display name from Google profile)
       - picture_url: Text, nullable=True (Google profile picture URL)
       - created_at, updated_at: DateTime(timezone=True) with same lambda pattern as other models
       - Add comments on every column explaining its purpose

    4. Create backend/app/auth.py with JWT utilities:
       - create_access_token(user_id: str, email: str) -> str: Creates a JWT with sub=user_id, email=email, exp=now+JWT_EXPIRY_HOURS using python-jose. Import settings from app.config.
       - get_current_user_optional(request: Request, db: Session = Depends(get_db)) -> User | None: Reads Authorization header, extracts Bearer token, decodes JWT, queries User by id. Returns None if no token or invalid token (does NOT raise — this is the optional version for routes that work both logged-in and anonymous).
       - get_current_user(user: User | None = Depends(get_current_user_optional)) -> User: Raises HTTPException(401) if user is None. Use this for routes that REQUIRE auth.
       - Add comments on every function explaining its purpose.

    5. Create backend/app/routers/auth.py with prefix="/api/auth" and tag="auth":
       - GET /google/login: Uses authlib to create an OAuth client for Google, generates the authorization URL with redirect_uri=http://localhost:8000/api/auth/google/callback, and returns RedirectResponse to Google's consent screen. Configure authlib OAuth client inline in the module (not in a separate file): create an OAuth() instance, register "google" with server_metadata_url="https://accounts.google.com/.well-known/openid-configuration", client_id and client_secret from settings.
       - GET /google/callback: Handles the OAuth callback. Uses authlib to exchange the authorization code for tokens, extracts user info (sub, email, name, picture) from the ID token. Queries DB for existing User by google_id — if found, updates name/picture_url; if not found, creates new User. Generates JWT via create_access_token. Returns RedirectResponse to http://localhost:3000?token={jwt_token} (frontend will extract token from URL query param).
       - GET /me: Protected with Depends(get_current_user). Returns the current user's id, email, name, picture_url as JSON.
       - POST /logout: No-op endpoint that returns {"ok": true}. JWT is stateless so logout is handled client-side by deleting the token. Include this endpoint for API completeness.
       - IMPORTANT: authlib's Starlette/FastAPI integration requires the request object. The OAuth authorize_redirect and authorize_access_token methods need the Request object passed in. Use `from starlette.requests import Request` in the route signatures.
       - IMPORTANT: authlib requires a session secret for OAuth state. Add `app.secret_key = settings.JWT_SECRET` or use SessionMiddleware. The simplest approach: in main.py, add `from starlette.middleware.sessions import SessionMiddleware` and `app.add_middleware(SessionMiddleware, secret_key=settings.JWT_SECRET)` BEFORE the CORS middleware.

    6. Update backend/app/main.py:
       - Add `import app.models.user  # noqa: F401` with the other model imports so the User table is created on startup.
       - Import and register the auth router: `from app.routers import auth` then `app.include_router(auth.router)` with a comment.
       - Add SessionMiddleware with settings.JWT_SECRET for authlib OAuth state management. Place it BEFORE CORSMiddleware. Import SessionMiddleware from starlette.middleware.sessions and settings from app.config.

    7. Add placeholder env vars to backend/.env (do NOT overwrite existing vars — append):
       - GOOGLE_CLIENT_ID=
       - GOOGLE_CLIENT_SECRET=
       - JWT_SECRET=dev-jwt-secret-change-in-production
  </action>
  <verify>
    <automated>cd /home/jiangpang/usmleAI/backend && pip install -r requirements.txt -q && python -c "from app.models.user import User; from app.auth import create_access_token, get_current_user_optional, get_current_user; from app.routers.auth import router; print('All imports OK')" && python -c "from app.config import settings; assert hasattr(settings, 'GOOGLE_CLIENT_ID'); assert hasattr(settings, 'JWT_SECRET'); print('Config OK')"</automated>
  </verify>
  <done>
    - User model exists in backend/app/models/user.py with google_id, email, name, picture_url columns
    - Auth utilities in backend/app/auth.py with create_access_token, get_current_user_optional, get_current_user
    - Auth router in backend/app/routers/auth.py with /api/auth/google/login, /callback, /me, /logout
    - main.py imports user model and registers auth router with SessionMiddleware
    - requirements.txt includes authlib and python-jose[cryptography]
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Auth context, Sign in button, token handling</name>
  <files>
    frontend/lib/types.ts,
    frontend/lib/auth.ts,
    frontend/lib/api.ts,
    frontend/components/dashboard/Dashboard.tsx
  </files>
  <action>
    1. Add auth types to frontend/lib/types.ts (append to end of file):
       ```typescript
       // ===== Auth Types =====

       /** User profile returned from GET /api/auth/me */
       export interface AuthUser {
         /** UUID of the user */
         id: string;
         /** User's email from Google */
         email: string;
         /** Display name from Google profile */
         name: string | null;
         /** Google profile picture URL */
         picture_url: string | null;
       }
       ```

    2. Create frontend/lib/auth.ts — a React context + provider for auth state:
       - AuthContext with { user: AuthUser | null, loading: boolean, login: () => void, logout: () => void }
       - AuthProvider component that:
         a. On mount, checks URL for ?token= query param (from OAuth callback redirect). If found, stores token in localStorage("usmleai_token"), removes ?token= from URL using window.history.replaceState (clean URL without reload).
         b. On mount (after token check), if localStorage has "usmleai_token", calls GET /api/auth/me with the token to get user profile. Sets user state on success, clears token on 401.
         c. login(): redirects window.location.href to `${API_BASE_URL}/api/auth/google/login`
         d. logout(): removes "usmleai_token" from localStorage, sets user to null.
         e. loading starts as true, set to false after initial auth check completes.
       - useAuth() hook that returns useContext(AuthContext) with a guard for undefined.
       - Export AuthProvider and useAuth.
       - Import API_BASE_URL from the api module or duplicate the env var logic.
       - Add comments on every function explaining its purpose.

    3. Update frontend/lib/api.ts — modify apiFetch to include auth token:
       - At the top of apiFetch, check if running in browser (typeof window !== "undefined") and get token from localStorage("usmleai_token").
       - If token exists, add Authorization: `Bearer ${token}` to the headers object (merge with existing headers).
       - This makes auth automatic on all API calls without changing any existing call sites.
       - Also export API_BASE_URL (change from const to export const) so auth.ts can import it.

    4. Update frontend/components/dashboard/Dashboard.tsx:
       - Import useAuth from "@/lib/auth".
       - In the header bar (the navy <header> element), add auth UI to the right side:
         - If loading: show nothing extra (or a subtle spinner).
         - If user is logged in: show user's name (or email if no name) and a "Sign out" button. If picture_url exists, show a small round avatar image (24x24).
         - If not logged in: show a "Sign in with Google" button styled with white text on the navy header, with a subtle border. On click, call login().
       - Use flex justify-between on the header's inner div to push auth UI to the right.
       - Do NOT wrap Dashboard in AuthProvider here — that goes in the layout.

    5. Wrap the app with AuthProvider in the root layout. Find the root layout file (likely frontend/app/layout.tsx) and wrap {children} with <AuthProvider>. Import AuthProvider from "@/lib/auth". Since AuthProvider uses hooks (useState, useEffect), and layout.tsx may be a server component, create a small client wrapper component if needed: frontend/components/providers/AuthProviderWrapper.tsx that re-exports AuthProvider as a "use client" component. OR if layout.tsx already has "use client", just add AuthProvider directly.
  </action>
  <verify>
    <automated>cd /home/jiangpang/usmleAI/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - AuthUser type exists in types.ts
    - Auth context provider in lib/auth.ts with login/logout/user state and token-from-URL extraction
    - apiFetch automatically attaches Bearer token from localStorage
    - Dashboard header shows "Sign in with Google" when logged out, user info + "Sign out" when logged in
    - Root layout wraps children in AuthProvider
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Backend starts without errors: `cd backend && uvicorn app.main:app --reload` (should show no import errors)
2. Users table is created in PostgreSQL on startup (check via psql or app logs)
3. GET /api/auth/google/login returns a redirect (302) to Google's OAuth consent screen (will fail without real credentials but should construct the URL)
4. Frontend compiles: `cd frontend && npm run build` succeeds
5. Dashboard shows "Sign in with Google" button in the header
6. After OAuth flow (with real Google credentials), user is redirected back, JWT stored, and header shows user info
</verification>

<success_criteria>
- User table exists in PostgreSQL with google_id, email, name, picture_url
- OAuth flow: /api/auth/google/login redirects to Google, callback creates user + returns JWT
- Frontend stores JWT in localStorage, sends it on all API requests via Authorization header
- Dashboard header shows sign-in button (anonymous) or user info (authenticated)
- Sign out clears token and returns to anonymous state
- All existing functionality continues to work (no regressions from optional auth)
</success_criteria>

<output>
After completion, create `.planning/quick/260320-l75-add-google-oauth-login-with-users-table-/260320-l75-SUMMARY.md`
</output>
